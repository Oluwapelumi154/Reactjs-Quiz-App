{/*return (
    <div className="questions-container">
     
      <div className="cont mt-4">
        <div className="container">
       {question.map((el,id)=>{
        
         return(<div key={id}>{el.question}
         <form name="demo">
           <label>
             <input name={id}  id="options" onChange={handleAnswer} answer={el.answer}   value={el.optionA}type="radio"/>
            {el.optionA}
           </label>
           <label>
             <input  name={id} id="options" onChange={handleAnswer} answer={el.answer}   value={el.optionB} type="radio"/>
            {el.optionB}
           </label>
           <label>
             <input  name={id} id="options"  onChange={handleAnswer} answer={el.answer}  value={el.optionC} type="radio"/>
            {el.optionC}
           </label>
           <label>
             <input  name={id}  id="options" onChange={handleAnswer}  answer={el.answer}   value={el.optionD} type="radio"/>
            {el.optionD}
           </label>
         </form>
         
         </div>)
       })}
        
      </div>
      </div>
   </div>  
      )*/}